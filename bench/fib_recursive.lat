// Benchmark: recursive fibonacci (exponential time, deep call stacks)
fn fib(n: Int) -> Int {
    if n <= 1 { return n }
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    let result = fib(32)
    print(to_string(result))
}
