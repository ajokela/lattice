export let VERSION = "1.0.0"
export let MAX_SIZE = 256

export fn greet(name: String) -> String {
    return "Hi, " + name
}

export fn compute(a: Int, b: Int) -> Int {
    return a * b + MAX_SIZE
}

fn private_helper() -> Int {
    return 42
}

let internal_state = "secret"
